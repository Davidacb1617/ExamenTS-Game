// Create the Planet
export class Planet {
    constructor(name, resourceType, dangerLevel) {
        this.name = name;
        this.resourceType = resourceType;
        this.dangerLevel = dangerLevel;
    }
}
